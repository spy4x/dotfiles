# Raised for a dev workload: many concurrent agent sessions, each editor,
# watcher and dev server opens its own inotify instance.
# Sorts after 50-kde-inotify-survey-max_user_instances.conf so it wins
# and kde-inotify-survey stops re-raising the limit and warning.
fs.inotify.max_user_instances = 1024
