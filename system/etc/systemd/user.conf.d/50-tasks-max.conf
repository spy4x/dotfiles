# Default task limit (processes plus threads) for every unit in the user session.
# Slices are exempt, so each app, service and scope keeps its own budget.
# See README, "Tasks per app".
[Manager]
DefaultTasksMax=16384
